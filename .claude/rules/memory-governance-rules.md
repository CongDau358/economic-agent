# Memory Rules

## The system must:

- preserve source traceability
- prioritize trusted knowledge
- avoid storing hallucinated outputs
- maintain metadata consistency

## The system must never:

- store unsupported claims
- overwrite validated knowledge without verification
