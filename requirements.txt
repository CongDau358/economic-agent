# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Config & settings ─────────────────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2        # NEW: replaces manual os.getenv
python-dotenv==1.0.1            # NEW: .env file support

# ── AI / LangChain ────────────────────────────────────────────────────────────
langchain==0.3.3
langchain-community==0.3.2
langchain-openai==0.2.2
openai==1.51.2

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb==0.4.22

# ── Document ingestion ────────────────────────────────────────────────────────
pypdf==5.0.1
pdfplumber==0.11.4
python-docx==1.1.2              # NEW: Word document support (was missing!)
openpyxl==3.1.5                 # Excel support
beautifulsoup4==4.12.3          # URL/HTML ingestion
 apscheduler==3.10.4
# ── Market data ───────────────────────────────────────────────────────────────
yfinance==0.2.43                # NEW: Yahoo Finance — free, no key needed
fredapi==0.5.2                  # NEW: FRED macro data (needs free API key)

# ── HTTP client ───────────────────────────────────────────────────────────────
requests==2.32.3
httpx==0.27.2                   # NEW: async HTTP, also used by tests

# ── Cache ─────────────────────────────────────────────────────────────────────
redis[asyncio]==5.0.8           # NEW: optional Redis cache

# ── Logging ───────────────────────────────────────────────────────────────────
# (structlog is optional; current impl uses stdlib + custom formatters)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3                   # NEW
pytest-asyncio==0.24.0          # NEW
pytest-mock==3.14.0             # NEW
pytest-cov==5.0.0               # NEW